# ============================================================================
# .env 文件示例 - 复制到 .env 并填写实际值
#
# ⚠️  注意：.env 包含敏感信息，不要提交到 Git！
#     请在 .gitignore 中添加：
#     .env
#     .env.local
#     .env.*.local
#
# ============================================================================

# ━━━━━━ 数据库配置 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# PostgreSQL 认证信息
DB_USER=zhinengxin
DB_PASSWORD=YourSecurePassword123!@#
DB_NAME=zhinengxin_ai

# ━━━━━━ 应用环境 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Node.js 环境
NODE_ENV=production
LOG_LEVEL=info

# ━━━━━━ 身份认证 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# JWT 密钥（必须长且复杂）
JWT_SECRET=your-super-secret-jwt-key-keep-it-safe-at-least-32-chars
JWT_EXPIRY=7d

# ━━━━━━ Redis 缓存 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Redis 密码
REDIS_PASSWORD=RedisSecurePassword123!
REDIS_URL=redis://:RedisSecurePassword123!@zhinengxin-cache:6379

# ━━━━━━ 第三方 API ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 聚宽（JoinQuant）API 凭证
JOINQUANT_API_KEY=your-joinquant-api-key
JOINQUANT_API_SECRET=your-joinquant-api-secret

# ━━━━━━ 前端配置 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 分析追踪 ID（可选）
VITE_ANALYTICS_ID=optional-tracking-id

# ━━━━━━ 容器端口映射 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 后端 API 服务端口
BACKEND_PORT=3000

# 前端 Web 服务端口
FRONTEND_PORT=80

# ============================================================================
# 【如何使用】
#
# 1. 复制此文件：
#    cp .env.example .env
#
# 2. 编辑 .env 填入实际值：
#    - 生成安全密码：openssl rand -base64 32
#    - 填入真实的 API 密钥
#
# 3. Docker Compose 会自动加载：
#    docker-compose -f docker-compose.production.yml up
#
# 4. 或显式指定：
#    docker-compose --env-file .env -f docker-compose.production.yml up
#
# ============================================================================
# 【密码生成建议】
#
# Linux/Mac:
#   openssl rand -base64 32   # 生成 32 字节随机密码
#
# PowerShell:
#   [Convert]::ToBase64String([System.Security.Cryptography.RNGCryptoServiceProvider]::new().GetBytes(32))
#
# ============================================================================
